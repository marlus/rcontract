<div id="group-form"></div>
<%= render :partial => 'form_items', :object => :group_form_items %>
<%= ext_form_for 'Group',
				 :title => 'Inserir Grupo',
                 :element => 'group-form',
                 :form_items => :group_form_items,
                 :mode => :new,
				 :tabId => params[:tabId], 
				 :tabTitle => params[:tabTitle] %>