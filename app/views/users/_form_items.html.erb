<%= select :user, :group_id,
       Group.find(:all).collect {|c| [ c.name, c.id ] },
       { :include_blank => true } %>
<% javascript_tag do -%>
  var <%= "#{form_items}" %> = [
    <%= ext_field(:field_label => 'Usuário', :name => 'user[username]', :xtype => :text_field) %>,
    <%= ext_field(:field_label => 'Senha', :name => 'user[password]', :xtype => :text_field) %>,
    <%= ext_field(:field_label => 'Nome', :name => 'user[name]', :xtype => :text_field) %>,
    <%= ext_field(:field_label => 'E-mail', :name => 'user[email]', :xtype => :text_field) %>,
    { fieldLabel:    'Grupo',
      xtype:         'combo',
      triggerAction: 'all',
      typeAhead:      true,
      forceSelection: true,
      transform:     'user_group_id',
      lazyRender:     true
    }
  ];
<% end -%>